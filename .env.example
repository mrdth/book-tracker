# Database Configuration
# For Docker: This is handled automatically via volume mounts
DATABASE_PATH=./data/books.db

# Hardcover API Configuration
# Get your API key from: https://hardcover.app/account/api
HARDCOVER_API_URL=https://api.hardcover.app/v1/graphql
HARDCOVER_API_KEY=your_api_key_here

# Book Collection Filesystem Path
# For Docker: Set this to the path on your HOST machine
# The path will be mounted as read-only inside the container
# Structure: {COLLECTION_ROOT}/Author name/Book title (ID)/
COLLECTION_ROOT=/path/to/your/book/collection

# Server Configuration
BACKEND_PORT=3034
FRONTEND_PORT=8174
NODE_ENV=production

# Frontend URLs
# For Docker: Use http://localhost:{BACKEND_PORT}
VITE_API_URL=http://localhost:3034
VITE_WS_URL=ws://localhost:3034
FRONTEND_URL=http://localhost:8174

# Logging Configuration
# Levels: debug, info, warn, error
LOG_LEVEL=info
